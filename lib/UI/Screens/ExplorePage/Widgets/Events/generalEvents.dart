import 'package:flutter/material.dart';

class GeneralEvents extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return (Container(
      child: Text('General'),
    ));
  }
}
