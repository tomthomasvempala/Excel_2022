import 'package:flutter/material.dart';

class GenTechCompetitions extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return (Container(
      child: Text('Gen Tech'),
    ));
  }
}
