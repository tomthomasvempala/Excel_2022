import 'package:flutter/material.dart';

class CSTechCompetitions extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return (Container(
      child: Text('CS Tech'),
    ));
  }
}
